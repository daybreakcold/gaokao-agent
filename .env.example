# 复制本文件为 .env 并填入真实 Key（.env 已被 .gitignore 忽略，不会被提交）
#   cp .env.example .env

# 必填：DeepSeek API Key，在 https://platform.deepseek.com 获取
DEEPSEEK_API_KEY=sk-你的key

# 可选：联网搜索优先使用 Tavily（更稳定），不配置则回退到免费的 DuckDuckGo
# TAVILY_API_KEY=tvly-你的key
